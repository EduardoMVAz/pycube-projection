NAME
    main - pycube: A simple 3D cube projection using Pygame and NumPy.

DESCRIPTION
    Classes:
    - Cube: Represents a 3D cube that can be rotated and projected onto 2D.

CLASSES
    builtins.object
        Cube

    class Cube(builtins.object)
     |  Cube(d=1)
     |
     |  Represents a 3D cube with projection and rotation functionality.
     |
     |  Attributes:
     |      WIDTH (int): Width of the Pygame window.
     |      LENGTH (int): Height of the Pygame window.
     |      FPS (int): Frames per second.
     |      screen: Pygame display surface.
     |      clock: Pygame clock object.
     |      d (float): Focal distance.
     |      projection_matrix (numpy.ndarray): Initial projection matrix.
     |
     |  Methods defined here:
     |
     |  __init__(self, d=1)
     |      Initialize the cube, Pygame window, and projection matrix.
     |
     |      Args:
     |          d (float, optional): Focal distance. Defaults to 1.
     |
     |  play(self)
     |      Run the main Pygame loop to display and manipulate the cube.
     |
     |      Handles user input for manual rotation, automatic spinning,
     |      zoom in/out, and reset.
     |
     |  quit()
     |
     |  rotation_matrix(self, angle, type='x')
     |      Return the rotation matrix for a given angle and axis.
     |
     |      Args:
     |          angle (float): Rotation angle in degrees.
     |          type (str): Rotation axis ('x', 'y', or 'z').
     |
     |      Returns:
     |          numpy.ndarray: 4x4 rotation matrix.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

FILE
    /home/eduardo/Desktop/opendev/pycube-projection/src/main.py


